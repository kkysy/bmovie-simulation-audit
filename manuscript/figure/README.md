# Figure generation

`generate_figures.py` is the public reproduction script. It writes rendered
figures as `Figure1`, `Figure2`, and so on, retaining informative extensions
such as `.png` and `.pdf`.

Rendered manuscript figures are generated locally and are intentionally not
included in this public release.
